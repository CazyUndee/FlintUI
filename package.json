{
  "name": "flintui-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "FlintUI - Multi-platform UI toolkit",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:web": "npm run build --workspace=flintui-web",
    "dev:web": "npm run dev --workspace=flintui-web"
  }
}
