using Microsoft.UI.Xaml;
using Microsoft.UI.Xaml.Controls;

namespace CronixUI.Controls;

public sealed class FlCheckBox : CheckBox
{
    public FlCheckBox()
    {
        DefaultStyleKey = typeof(FlCheckBox);
    }
}
